name: Reusable workflow test

on:
  workflow_call:
    inputs:
      SERVICE_NAME:
        required: true
        type: string
    secrets: 
      DEMO:
        required: true
      GH_TOKEN:
        required: false

jobs:
  # baker:
  #   name: Pass input to my-action
  #   runs-on: ubuntu-latest
  #   steps:
  #   - name: Checkout
  #     uses: actions/checkout@v2

  check_errors:
    name: Check errors
    runs-on: ubuntu-latest
    # needs: [baker]
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: check logs
      if: env.GH_TOKEN != ''
      run: |
        echo now: ${{ github.run_id }}
        runid="2585339531"
        gh run view $runid --log
      env:
        GH_TOKEN: ${{secrets.GH_TOKEN}}